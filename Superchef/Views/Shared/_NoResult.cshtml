@model string

<div id="no-result" class="container" data-no="color padding" data-flex="column center">
    <div class="icon">(˚Δ˚)b</div>
    <div class="text">@(Model ?? "No Result Found")</div>
</div>